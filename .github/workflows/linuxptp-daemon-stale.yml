name: "linuxptp-daemon: Close stale issues and PRs"
on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  stale:
    uses: k8snetworkplumbingwg/ptp-operator/.github/workflows/_reusable-linuxptp-daemon-stale.yml@main
