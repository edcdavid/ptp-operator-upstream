name: "linuxptp-daemon: Go"
on:
  push:
    branches: ["main", "one-repo*", "release-*"]
    paths:
      - 'linuxptp-daemon/**'
  pull_request:
    paths:
      - 'linuxptp-daemon/**'

jobs:
  build:
    uses: ./.github/workflows/_reusable-linuxptp-daemon-go.yml