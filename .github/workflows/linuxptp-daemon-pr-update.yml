name: "linuxptp-daemon: Run Jobs on PR"
on:
  pull_request:
    paths:
      - 'linuxptp-daemon/**'

jobs:
  pr-update:
    uses: k8snetworkplumbingwg/ptp-operator/.github/workflows/_reusable-linuxptp-daemon-pr-update.yml@main
    secrets: inherit
