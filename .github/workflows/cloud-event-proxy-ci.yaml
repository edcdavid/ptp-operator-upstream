name: "cloud-event-proxy: CI"
on:
  push:
    paths:
      - 'cloud-event-proxy/**'
  pull_request:
    paths:
      - 'cloud-event-proxy/**'

jobs:
  ci:
    uses: k8snetworkplumbingwg/ptp-operator/.github/workflows/_reusable-cloud-event-proxy-ci.yml@main
