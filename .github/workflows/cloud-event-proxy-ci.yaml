name: "cloud-event-proxy: CI"
on:
  push:
    paths:
      - 'cloud-event-proxy/**'
  pull_request:
    paths:
      - 'cloud-event-proxy/**'

jobs:
  ci:
    uses: ./.github/workflows/_reusable-cloud-event-proxy-ci.yml